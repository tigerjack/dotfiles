# Directory where the files will be synced
sync_dir = "/mnt/internal/DatiSimone/Public/onedrive"
# Skip files and directories that match this pattern
skip_file = ".*|~*"
# Skip simlinks
skip_symlinks = "false"
# Wait time (seconds) between sync operations in monitor mode
monitor_interval = "60"
